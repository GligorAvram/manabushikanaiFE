import { NgModule } from '@angular/core';
import { ReaderShellRoutingModule } from './reader-shell-routing.module';

@NgModule( {
               imports: [
                   ReaderShellRoutingModule
               ]
           } )
export class ReaderShellModule {
}
